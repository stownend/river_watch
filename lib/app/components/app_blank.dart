import 'package:flutter/material.dart';

class AppBlank extends StatelessWidget {
  const AppBlank({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const []
    ); 
  }
}